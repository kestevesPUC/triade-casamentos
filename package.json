{
  "name": "triade-casamentos",
  "version": "1.0.0",
  "private": true,
  "description": "Site institucional da Triade Casamentos - musicos para casamentos em BH e regiao",
  "scripts": {
    "dev": "npm run dev --workspace client",
    "dev:api": "npm run dev --workspace server",
    "build": "npm run build --workspace client",
    "preview": "npm run preview --workspace client"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "engines": {
    "node": ">=18"
  }
}
