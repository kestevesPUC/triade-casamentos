# SMTP opcional para o formulário de contato (api/contact.js).
# Sem essas variáveis, os leads apenas são registrados no log da função.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
CONTACT_TO_EMAIL=
